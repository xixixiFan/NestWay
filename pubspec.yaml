name: solotrip
description: NestWay - 女性独旅安全 App
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  video_player: ^2.11.0
  http: ^1.2.0
  supabase_flutter: ^2.10.0
  sign_in_with_apple: ^8.0.0
  provider: ^6.1.2
  geolocator: ^13.0.2
  permission_handler_html: ^0.1.3+5
  image_picker: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/attention_video.mp4
