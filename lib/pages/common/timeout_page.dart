import 'package:flutter/material.dart';

class TimeoutPage extends StatelessWidget {
  const TimeoutPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('超时页面')),
    );
  }
}