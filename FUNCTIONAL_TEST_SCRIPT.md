# 栖途 NestWay - 前后端联调功能测试脚本

**项目**：栖途 NestWay 女性独旅安全 App  
**测试环境**：Supabase Cloud + Flutter App  
**版本**：v1.0  
**日期**：2026-05-16

---

## 目录

1. [测试前准备](#1-测试前准备)
2. [数据库验证测试](#2-数据库验证测试)
3. [用户认证测试](#3-用户认证测试)
4. [SOS 核心功能测试](#4-sos-核心功能测试)
5. [紧急联系人测试](#5-紧急联系人测试)
6. [虚拟护送功能测试](#6-虚拟护送功能测试)
7. [测试结果记录](#7-测试结果记录)

---

## 1. 测试前准备

### 1.1 检查 Supabase 配置

**检查项**：
- [ ] Phone Authentication 已启用
- [ ] Test Phone Numbers 已配置
- [ ] API URL 正确：`https://fbnctnhjcjkbmmvcuqxh.supabase.co`

### 1.2 检查数据库表

在 Supabase **Table Editor** 中验证以下表是否存在：

| 表名 | 预期字段 | 检查 |
|------|---------|------|
| users | id, name, phone, avatar_url, is_verified, created_at | [ ] |
| emergency_contacts | id, user_id, name, phone, sort_order, created_at | [ ] |
| sos_logs | id, user_id, type, location_description, latitude, longitude, triggered_at | [ ] |
| escort_tasks | id, user_id, start_location, end_location, estimated_duration, status, started_at, completed_at, last_location_lat, last_location_lng, created_at | [ ] |

### 1.3 检查测试数据

在 Supabase **Table Editor** 中验证测试数据：

```sql
-- 检查 users 表
SELECT * FROM users;
-- 预期：1 条记录（范颖）

-- 检查 emergency_contacts 表
SELECT * FROM emergency_contacts;
-- 预期：3 条记录（妈妈、爸爸、室友）

-- 检查 sos_logs 表
SELECT * FROM sos_logs;
-- 预期：3 条记录（东京涩谷、新宿、池袋）
```

---

## 2. 数据库验证测试

### 2.1 测试用户表读取

**测试目标**：验证前端能正确读取 users 表数据

**测试步骤**：
1. 在 Supabase **Table Editor** 中打开 `users` 表
2. 记录第一条数据的 `id`
3. 运行应用，查看是否能连接数据库

**预期结果**：
- ✅ 连接成功
- ✅ 能读取到用户"范颖"的数据

**实际结果**：
```
[请在此记录测试结果]
```

---

### 2.2 测试 SOS 日志表读取

**测试目标**：验证前端能正确读取 sos_logs 表数据

**测试步骤**：
1. 在 Supabase **Table Editor** 中打开 `sos_logs` 表
2. 记录所有数据的 `id` 和 `type`
3. 在应用中进入 SOS 页面
4. 点击右上角"历史"图标

**预期结果**：
- ✅ 能读取到 3 条 SOS 记录
- ✅ 显示类型：call、sms、video
- ✅ 显示位置描述：东京涩谷站附近、新宿歌舞伎町、池袋西口

**实际结果**：
```
[请在此记录测试结果]
```

---

## 3. 用户认证测试

### 3.1 测试号码配置

**测试目标**：配置测试手机号和验证码

**测试步骤**：
1. 在 Supabase Dashboard 中进入 **Authentication** → **Settings**
2. 在 **Phone** 部分填写：

```
Test Phone Numbers and OTPs:
+8613800138000=123456
```

3. 保存配置

**预期结果**：
- ✅ 配置保存成功
- ✅ 无错误提示

**实际结果**：
```
[请在此记录测试结果]
```

---

### 3.2 测试登录页面

**测试目标**：验证登录页面能正常显示和交互

**测试步骤**：
1. 打开应用
2. 如果有登录按钮，点击进入登录页面
3. 如果没有，检查首页是否有 SOS 功能入口

**预期结果**：
- ✅ 页面正常显示
- ✅ 输入框可输入
- ✅ 按钮可点击

**实际结果**：
```
[请在此记录测试结果]
```

---

### 3.3 测试验证码登录（可选）

**测试目标**：使用测试号码和验证码登录

**测试步骤**：
1. 输入手机号：`13800138000`
2. 点击"获取验证码"
3. 输入验证码：`123456`
4. 点击"确认验证"

**预期结果**：
- ✅ 收到验证成功提示
- ✅ 跳转到首页或 SOS 页面

**实际结果**：
```
[请在此记录测试结果]
```

---

## 4. SOS 核心功能测试

### 4.1 测试 SOS 历史记录显示

**测试目标**：验证 SOS 历史记录页面能正确显示数据库中的数据

**测试步骤**：
1. 进入应用 SOS 页面
2. 点击右上角"历史"图标
3. 查看历史记录列表

**检查项**：
- [ ] 是否显示 3 条记录？
- [ ] 记录类型是否正确（call、sms、video）？
- [ ] 位置描述是否正确？
- [ ] 时间显示是否正确？

**预期结果**：
| 序号 | 类型 | 位置描述 | 时间 |
|------|------|---------|------|
| 1 | call | 东京涩谷站附近 | 2025-05-01 |
| 2 | sms | 新宿歌舞伎町 | 2025-05-02 |
| 3 | video | 池袋西口 | 2025-05-03 |

**实际结果**：
```
[请在此记录测试结果]
```

---

### 4.2 测试紧急报警按钮

**测试目标**：验证长按紧急报警按钮的功能

**测试步骤**：
1. 进入 SOS 页面
2. 找到红色"紧急报警"卡片
3. 长按 3 秒
4. 观察进度动画
5. 松开手指取消

**检查项**：
- [ ] 长按是否显示进度动画？
- [ ] 进度是否从 0% 到 100%？
- [ ] 松开是否取消？
- [ ] 完成时是否弹出确认对话框？

**预期结果**：
- ✅ 红色卡片上有进度圆环
- ✅ 显示百分比数字
- ✅ 松开手指，进度消失
- ✅ 完成时弹出"确认拨打110"对话框

**实际结果**：
```
[请在此记录测试结果]
```

---

### 4.3 测试发送求助短信

**测试目标**：验证发送求助短信功能

**测试步骤**：
1. 进入 SOS 页面
2. 点击黄色"共享位置给联系人"卡片
3. 查看页面内容
4. 选择紧急联系人
5. 点击"发送"按钮

**检查项**：
- [ ] 是否显示收件人下拉框？
- [ ] 是否显示短信预览？
- [ ] 是否显示"发送"按钮？
- [ ] 点击发送后是否有提示？

**预期结果**：
| 检查项 | 预期 | 实际 |
|--------|------|------|
| 收件人选择 | 显示"妈妈"（默认） | [ ] |
| 短信内容 | 显示求助消息模板 | [ ] |
| 发送按钮 | 可点击 | [ ] |
| 发送成功 | 显示绿色成功对话框 | [ ] |

**实际结果**：
```
[请在此记录测试结果]
```

---

### 4.4 测试数据插入功能

**测试目标**：验证 SOS 事件能正确写入数据库

**测试步骤**：
1. 发送一条求助短信
2. 观察是否显示成功提示
3. 进入 Supabase **Table Editor**
4. 打开 `sos_logs` 表
5. 查看是否有新记录

**预期结果**：
- ✅ 显示发送成功
- ✅ 数据库中有新的一条记录
- ✅ type 为 'sms'
- ✅ triggered_at 为当前时间

**实际结果**：
```
[请在此记录测试结果]
```

---

## 5. 紧急联系人测试

### 5.1 测试紧急联系人读取

**测试目标**：验证紧急联系人能从数据库正确读取

**测试步骤**：
1. 在 Supabase **Table Editor** 中打开 `emergency_contacts` 表
2. 确认有 3 条联系人数据
3. 在应用中点击"共享位置给联系人"
4. 查看联系人下拉框

**预期结果**：
| 序号 | 姓名 | 电话 | 排序 |
|------|------|------|------|
| 1 | 妈妈 | 13900000001 | 1 |
| 2 | 爸爸 | 13900000002 | 2 |
| 3 | 室友 | 13900000003 | 3 |

**实际结果**：
```
[请在此记录测试结果]
```

---

### 5.2 测试联系人选择

**测试目标**：验证能正确切换紧急联系人

**测试步骤**：
1. 在发送短信页面
2. 点击收件人下拉框
3. 选择不同联系人（如"爸爸"）
4. 观察页面变化

**预期结果**：
- ✅ 下拉框显示所有联系人
- ✅ 选择后显示选中联系人的姓名
- ✅ 短信内容不变

**实际结果**：
```
[请在此记录测试结果]
```

---

## 6. 虚拟护送功能测试

### 6.1 测试护送任务表存在性

**测试目标**：验证 escort_tasks 表已正确创建

**测试步骤**：
1. 在 Supabase **Table Editor** 中查找 `escort_tasks` 表
2. 查看表结构是否正确

**预期结果**：
- ✅ 表存在
- ✅ 包含所有预期字段

**实际结果**：
```
[请在此记录测试结果]
```

---

### 6.2 测试护送页面入口

**测试目标**：验证虚拟护送功能入口存在

**测试步骤**：
1. 在首页查找"虚拟护送"入口
2. 或在底部导航查找相关按钮
3. 查看页面结构

**预期结果**：
- ✅ 存在虚拟护送功能入口
- ✅ 能正常进入护送设置页面

**实际结果**：
```
[请在此记录测试结果]
```

---

## 7. 测试结果记录

### 7.1 测试汇总表

| 序号 | 测试项 | 状态 | 问题描述 | 严重程度 |
|------|--------|------|---------|---------|
| 1 | 数据库表创建 | [ ] 通过 [ ] 失败 | | |
| 2 | 测试数据插入 | [ ] 通过 [ ] 失败 | | |
| 3 | 用户表读取 | [ ] 通过 [ ] 失败 | | |
| 4 | SOS日志表读取 | [ ] 通过 [ ] 失败 | | |
| 5 | SOS历史页面显示 | [ ] 通过 [ ] 失败 | | |
| 6 | 紧急报警按钮 | [ ] 通过 [ ] 失败 | | |
| 7 | 发送求助短信 | [ ] 通过 [ ] 失败 | | |
| 8 | 数据插入功能 | [ ] 通过 [ ] 失败 | | |
| 9 | 紧急联系人读取 | [ ] 通过 [ ] 失败 | | |
| 10 | 联系人选择功能 | [ ] 通过 [ ] 失败 | | |
| 11 | 护送任务表存在 | [ ] 通过 [ ] 失败 | | |
| 12 | 护送页面入口 | [ ] 通过 [ ] 失败 | | |

---

### 7.2 严重程度定义

| 级别 | 标识 | 说明 |
|------|------|------|
| 致命 | 🔴 | 功能完全不可用，无法继续测试 |
| 严重 | 🟠 | 功能有重大缺陷，但可以绕过 |
| 一般 | 🟡 | 功能有小问题，不影响核心流程 |
| 提示 | 🔵 | 建议改进，非功能性问题 |

---

### 7.3 遇到的问题

```
[请在此记录所有遇到的问题]
```

---

### 7.4 优化建议

```
[请在此记录优化建议]
```

---

## 附录 A：快速测试命令

### A.1 检查 Supabase 连接

```dart
// 在应用中添加测试代码
import 'package:supabase_flutter/supabase_flutter.dart';

Future<void> testConnection() async {
  final response = await Supabase.instance.client.from('users').select();
  print('连接成功: ${response.data}');
}
```

### A.2 测试数据查询

```dart
// 测试 SOS 历史
final sosLogs = await Supabase.instance.client
    .from('sos_logs')
    .select()
    .order('triggered_at', ascending: false);
print('SOS 历史: $sosLogs');

// 测试紧急联系人
final contacts = await Supabase.instance.client
    .from('emergency_contacts')
    .select()
    .order('sort_order');
print('紧急联系人: $contacts');
```

### A.3 测试数据插入

```dart
// 测试插入 SOS 日志
await Supabase.instance.client.from('sos_logs').insert({
  'type': 'sms',
  'location_description': '测试位置',
  'triggered_at': DateTime.now().toIso8601String(),
});
```

---

## 附录 B：Supabase API 测试

### B.1 使用 Supabase Dashboard SQL Editor

```sql
-- 测试用户查询
SELECT * FROM users LIMIT 5;

-- 测试 SOS 日志查询
SELECT * FROM sos_logs ORDER BY triggered_at DESC LIMIT 10;

-- 测试紧急联系人查询
SELECT * FROM emergency_contacts WHERE user_id = 1 ORDER BY sort_order;

-- 测试数据插入
INSERT INTO sos_logs (type, location_description)
VALUES ('test', '测试插入');

-- 验证插入
SELECT * FROM sos_logs WHERE type = 'test';
```

---

**测试脚本完成时间**：2026-05-16  
**测试人员**：  
**审核人员**：

---

**下一步行动**：

1. [ ] 执行所有测试项
2. [ ] 记录测试结果
3. [ ] 汇总问题清单
4. [ ] 优先修复致命和严重问题
5. [ ] 重新测试修复项